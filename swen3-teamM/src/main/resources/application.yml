spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/paperless
    username: test
    password: paper
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 50MB
server:
  port: 8080

#ToDo Paths einfügen
#Beim Curl ist es mir aber ein Rätsel